'use strict';

/**
 * opportunity-status router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::opportunity-status.opportunity-status');
