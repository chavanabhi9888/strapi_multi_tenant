'use strict';

/**
 * opportunity-status service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::opportunity-status.opportunity-status');
